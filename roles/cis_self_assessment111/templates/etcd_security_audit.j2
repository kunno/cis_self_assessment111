{%- extends "cp_security_audit.j2" -%}
{%- block cis_self_assessment111 -%}
{{ super() }}
{%- endblock -%}
{%- block etcd_security_audit -%}
{%- if 'etcd_audit' in ansible_run_tags or 'etcd_security_audit' in ansible_run_tags or 'etcd_config_audit' in ansible_run_tags or 'all' in ansible_run_tags -%}
{%- raw %}
# 2 Etcd Node Configuration

## 2 Etcd Node Configuration

### 2.1 Ensure that the --cert-file and --key-file arguments are set as appropriate (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['client-transport-security']['cert-file'] is defined -%}
{%- if etcd_config_data['client-transport-security']['key-file'] is defined -%}
{%- if etcd_config_data['client-transport-security']['cert-file'] == '/var/lib/rancher/rke2/server/tls/etcd/server-client.crt' %}
{%- if etcd_config_data['client-transport-security']['key-file'] == '/var/lib/rancher/rke2/server/tls/etcd/server-client.key' %}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `Cert: {{ etcd_config_data['client-transport-security']['cert-file']|string + '` ' }} `Key: {{ etcd_config_data['client-transport-security']['key-file']|string + '` ' }}
{%- raw -%}
Desired Setting: **Cert: /var/lib/rancher/rke2/server/tls/etcd/server-client.crt Key: /var/lib/rancher/rke2/server/tls/etcd/server-client.key**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.2 Ensure that the --client-cert-auth argument is set to true (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['client-transport-security']['client-cert-auth'] is defined -%}
{%- if etcd_config_data['client-transport-security']['client-cert-auth']|bool == true %}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `{{ etcd_config_data['client-transport-security']['client-cert-auth']|string + '` ' }}
{%- raw -%}
Desired Setting: **true**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.3 Ensure that the --auto-tls argument is not set to true (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['client-transport-security']['auto-tls'] is defined -%}
{%- if etcd_config_data['client-transport-security']['auto-tls']|bool == false -%}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `{{ etcd_config_data['client-transport-security']['auto-tls']|string + '` ' }}
{%- raw -%}
Desired Setting: **false**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.4 Ensure that the --peer-cert-file and --peer-key-file arguments are set as appropriate (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['peer-transport-security']['cert-file'] is defined -%}
{%- if etcd_config_data['peer-transport-security']['key-file'] is defined -%}
{%- if etcd_config_data['peer-transport-security']['cert-file'] == '/var/lib/rancher/rke2/server/tls/etcd/peer-server-client.crt' %}
{%- if etcd_config_data['peer-transport-security']['key-file'] == '/var/lib/rancher/rke2/server/tls/etcd/peer-server-client.key' %}
 PASS  
{% else %}
 FAIL  
{% endif %}
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `Cert: {{ etcd_config_data['peer-transport-security']['cert-file']|string + '` ' }} `Key: {{ etcd_config_data['peer-transport-security']['key-file']|string + '` ' }}
{%- raw -%}
Desired Setting: **Cert: /var/lib/rancher/rke2/server/tls/etcd/peer-server-client.crt Key: /var/lib/rancher/rke2/server/tls/etcd/peer-server-client.key**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.5 Ensure that the --peer-client-cert-auth argument is set to true (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['peer-transport-security']['client-cert-auth'] is defined -%}
{%- if etcd_config_data['peer-transport-security']['client-cert-auth']|bool == true %}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `{{ etcd_config_data['peer-transport-security']['client-cert-auth']|string + '` ' }}
{%- raw -%}
Desired Setting: **true**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.6 Ensure that the --peer-auto-tls argument is not set to true (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['peer-transport-security']['auto-tls'] is defined -%}
{%- if etcd_config_data['peer-transport-security']['auto-tls']|bool == false -%}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `{{ etcd_config_data['peer-transport-security']['auto-tls']|string + '` ' }}
{%- raw -%}
Desired Setting: **false**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{% raw %}
### 2.7 Ensure that a unique Certificate Authority is used for etcd (Automated)

**Audit:**  
`cat /var/lib/rancher/rke2/server/db/etcd/config`

**Result:**

{%- endraw -%}

{%- if etcd_config_data['peer-transport-security']['trusted-ca-file'] is defined -%}
{%- if etcd_config_data['peer-transport-security']['trusted-ca-file'] == '/var/lib/rancher/rke2/server/tls/etcd/peer-ca.crt' %}
 PASS  
{% else %}
 FAIL  
{% endif %}
{%- raw -%}
**Current Setting:**
{%- endraw %}
 `Cert: {{ etcd_config_data['peer-transport-security']['cert-file']|string + '` ' }}
{%- raw -%}
Desired Setting: **Trusted CA: /var/lib/rancher/rke2/server/tls/etcd/peer-ca.crt**
{% endraw %}
{%- else %}
 FAIL  
{% endif %}
{% endif %}
{% endblock %}
